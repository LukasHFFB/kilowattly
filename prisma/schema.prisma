generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Category {
  id          String       @id @default(cuid())
  name        String       @unique
  calculators Calculator[]
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt
}

model Calculator {
  id                        String   @id @default(cuid())
  slug                      String   @unique
  keyword                   String
  deviceName                String
  default_wattage           Int
  average_daily_usage_hours Float
  price_cents               Float    @default(35.0)
  seo_content               String   // HTML or string
  
  categoryId                String?
  category                  Category? @relation(fields: [categoryId], references: [id])
  
  faqs                      String   // JSON string of FAQs

  // Real User Ratings
  realRatingSum           Int      @default(0)
  realRatingCount         Int      @default(0)
  
  status                    String   @default("DRAFT") // DRAFT, PUBLISHED
  
  createdAt                 DateTime @default(now())
  updatedAt                 DateTime @updatedAt
}

model SystemPrompt {
  id        String   @id // "LOGIC" or "CONTENT"
  template  String   // The text of the prompt
  updatedAt DateTime @updatedAt
}
